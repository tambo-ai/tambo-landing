# Created by Vercel CLI
NEXT_PUBLIC_BASE_URL="Base URL"
NEXT_PUBLIC_TAMBO_API_KEY="Your tambo API key"
NEXT_PUBLIC_LOGIN_URL="Login CTA URL"
# Mapbox (disabled by default for performance - ~200KB savings)
NEXT_PUBLIC_MAPBOX_ENABLED=false
NEXT_PUBLIC_MAPBOX_TOKEN="Your public mapbox token"
MAPBOX_TOKEN="it can be the same as NEXT_PUBLIC_MAPBOX_TOKEN but gives the possibility of using specific API tokens"

# PostHog Analytics (optional - omit for local/OSS deployments)
# Uses reverse proxy at /ingest for more accurate tracking
NEXT_PUBLIC_POSTHOG_KEY=
